package ua.zakirolek.mockito.model.dao;

/*
import java.util.Arrays;
import java.util.List;

public class UserDaoImpl implements UserDao {
    private List<User>users;

    public UserDaoImpl() {
        this.users = Arrays.asList(
                new User("olesia@gmail.com", "GUEST"),
                new User("kiril@gmail.com", "USER"),
                new User("remy@gmail.com", "ADMIN")
        );
    }

    @Override
    public ua.zakirolek.mockito.model.model.User getUserByUserName(String username) throws Exception {
        return users.stream().filter(u->u.getUsername().equals(username));
        findAny().orElse(null);

    }

    private void findAny() {
    }
}
*/
